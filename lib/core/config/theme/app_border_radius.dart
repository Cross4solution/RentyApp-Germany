import '../../_core_exports.dart';

class AppBorderRadius {
  static const BorderRadius borderRadiusAll_6px = BorderRadius.all(Radius.circular(6));
  static const BorderRadius borderRadiusAll_12px = BorderRadius.all(Radius.circular(12));
  static const BorderRadius borderRadiusAll_13px = BorderRadius.all(Radius.circular(13));
  static const BorderRadius borderRadiusAll_14px = BorderRadius.all(Radius.circular(14));
  static const BorderRadius borderRadiusAll_16px = BorderRadius.all(Radius.circular(16));
  static const BorderRadius borderRadiusAll_24px = BorderRadius.all(Radius.circular(24));
}
